# Optional: ControlNet + Stable Diffusion inpainting render mode.
#
# NOT installed in the default (CPU) Docker image to keep it small. Runs on a
# CUDA GPU (recommended) or on CPU (slow, ~1-3 min per render). Weights download
# on the first ControlNet render (~2-4 GB, cached in the HF cache).
#
# Install into the same environment as the base backend:
#
#   # (GPU) install a CUDA torch build first, e.g.:
#   # pip install torch --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#   pip install -r requirements-diffusion.txt
#
# Then pick "ControlNet (AI)" in the UI, or set DEFAULT_RENDER_MODE=controlnet.

diffusers>=0.27.0
accelerate>=0.30.0
safetensors>=0.4.2
# transformers and torch are already pinned in requirements.txt.
